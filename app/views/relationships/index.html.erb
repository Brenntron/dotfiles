<div class='row item-info'>
  <div class="row subheading">
    <button class="btn back_btn_user pull-right">
      <i class="glyphicon glyphicon-arrow-left"></i>
      <%=link_to ' Back', users_path%>
    </button>
  </div>
  <div class='col-lg-11 col-md-11'>
    <div class="row">
      <div class="col-xs-12 col-sm-6">
        <h2 class="subtitle"> <%="#{current_user.display_name}'s Team"%>
          <span class="email-link"><%= image_tag "tree-diagram-icon.png" %></span>
        </h2>
      </div>
    </div>
    <div class='row top-space'>
      <div class="col-xs-11">
        <div class="col-xs-6 col-sm-4">
          <% if @user.team_members.any? %>
              <table class="table table-hover table-sm table-borderless">
                <tbody>
                  <% @user.team_member_relationships.each do |member| %>
                    <tr>
                      <th><%= link_to member.team_member.cvs_username, user_path(member.team_member) %></th>
                      <th><%= link_to '<i class="glyphicon glyphicon-remove-circle"></i>'.html_safe, user_relationship_path(id: member.id), method: :delete, data: {confirm: "Remove #{member.team_member.display_name} from your team?"}%></th>
                    </tr>
                  <%end%>
                </tbody>
              </table>
          <% else %>
              <p class="text-muted small"><em><strong><%= @user.cvs_username %></strong> is a lone wolf!</em></p>
          <% end %>
          <br>
          <%=form_for :relationship, url: user_relationships_path(@user), method: :post do |f|%>
            <div class="form-group"><div class="form-inline">
              <%= f.select :team_member_id, options_for_select(@users.map{|u| ["#{u.display_name} (#{u.cvs_username})", u.id]}), {prompt: "Choose Team Member"}, class: "form-control", required: "true"%>
              <%= f.hidden_field :user_id, value: @user.id %>
              <%= f.submit 'Add', class: 'btn btn-primary' %>
              <p class="help-block text-nowrap">Any member of a team can view other members of the team(s) they belong to.</p>
            </div>
            </div>
          <%end%>
        </div>
      </div>
    </div>
    <div class="row">
      <div class="col-xs-11"><hr/></div>
      <div class='col-xs-12'>
        <div class='col-xs-11'>
          <h4>Metrics <a href="#" class="tiny text-muted change_metrics_timeframe"><em>change timeframe</em></a></h4>
          <%= render 'shared/change_metrics_timeframe_form' %>
          <div id="msg"></div>
        </div>
      </div>
      <div class="col-lg-5 col-md-6">
        <div class="user-metrics-manager" id='<%=current_user.id%>'></div>
        <p>Bug status set to <i>Pending</i> last <%=current_user.chart_timeframe_preference%> days</p>
        <canvas id="pendingChart"></canvas>
      </div>
      <div class="col-lg-5 col-md-6">
        <p>Bug status set to <i>Resolved</i> last <%=current_user.chart_timeframe_preference%> days</p>
        <canvas id="resolvedChart"></canvas>
      </div>
    </div>
    <div class="row">
      <div class="col-xs-11"><hr/></div>
      <div class='col-xs-12'>
        <div class='col-xs-11'>
          <div id="msg"></div>
        </div>
      </div>
      <div class="col-lg-5 col-md-6">
        <p>Average Work Time by Team Memeber</p>
        <canvas id="worktimeChart"></canvas>
        <br>
        <div class="col-xs-12">
          <p class="text-muted small"><em>* Averages rounded to the nearest whole number.</em></p>
        </div>
      </div>
      <div class="col-lg-5 col-md-6">
        <p>Average Work Time by Component</p>
        <canvas id="componenttimeChart"></canvas>
        <br>
        <div class="col-xs-12">
          <p class="text-muted small"><em>* Averages rounded to the nearest whole number.</em></p>
        </div>
      </div>
    </div>
    <div class="footer-spacer"></div>
    <input type="hidden" name="token" value="<%= current_user.authentication_token %>">
    <input type="hidden" name="xml_token" value="<%= xml_token %>">
    <input type="hidden" name="current_user" value="<%= current_user.id%>">
  </div>
</div>

<div id="new_member_modal" class="modal" role="dialog">
  <div class="modal-dialog modal-sm">
    <div class="modal-content">
      <div class="modal-header user">
        <h4 class="modal-title">
          Information Alert
        </h4>
      </div>
      <div class="modal-body center-block">
      </div>
      <div class="modal-footer">
        <button type="button" class="btn btn-primary" data-dismiss="modal">Ok</button>
      </div>
    </div>
  </div>
</div>
