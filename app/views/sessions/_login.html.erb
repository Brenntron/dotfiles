<div id="login-flash"><%= render partial: "shared/flash_messages", flash: flash %></div>
<div class="jumbotron" id="login-area">
  <div class="col-sm-offset-2">
    <p class="tagline">

      <% if Rails.env == "production" %>
          Please sign in using your CEC credentials
      <% else %>
          Please sign in using your Cisco credentials
      <% end %>
      <%= form_tag(escalations_sessions_path, method: :post, :content_type => 'application/json') do -%>
        <div class="container">
          <label><b>Username</b></label>
          <input type="text" placeholder="Enter Username" name="uname" required>

          <label><b>Password</b></label>
          <input type="password" placeholder="Enter Password" name="psw" required>

          <button type="submit">Login</button>
        </div>
    <% end %>

    </p>
  </div>
</div>
