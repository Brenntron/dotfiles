<div class='row item-info'>
  <div class='col-lg-11 col-md-11'>
    <div class="row">
      <div class="col-xs-12 col-sm-4">
        <h2 class="subtitle"><%= @user.cvs_username %>&nbsp;&nbsp;
          <span class="email-link"><a href="mailto:<%= @user.email %>"><i class="glyphicon glyphicon-envelope"></i></a></span>
        </h2>
      </div>
    </div>
    <div class='row top-space'>
      <div class="col-xs-11">
        <div class="col-xs-12">
          <h4>Team</h4>
        </div>
        <div class="col-xs-6 col-sm-4">
          <h6 class="text-muted">managers</h6>
          <% if @user.managers.any? %>
            <ul class="compact-list">
              <% @user.managers.each do |manager| %>
                <li><%= link_to manager.cvs_username, user_path(manager) %></li>
              <% end %>
            </ul>
          <% else %>
            <p class="text-muted small"><em><strong><%= @user.cvs_username %></strong> answers to no one!</em></p>
          <% end %>
        </div>
        <div class="col-xs-6 col-sm-4">
          <h6 class="text-muted">team</h6>
          <% if @user.team_members.any? %>
            <ul class="compact-list">
              <% @user.team_members.each do |member| %>
              <li><%= link_to member.cvs_username, user_path(member) %></li>
              <% end %>
            </ul>
          <% else %>
            <p class="text-muted small"><em><strong><%= @user.cvs_username %></strong> is a lone wolf!</em></p>
          <% end %>
        </div>
      </div>
    </div>
    <div class="row">
      <div class="col-xs-11"><hr/></div>
      <div class='col-xs-12'>
        <div class='col-xs-11'>
          <h4>Assignments</h4>
        </div>
      </div>
      <div class='col-sm-11'>
        <ul class="nav nav-tabs">
          <li role="presentation" class="user-tab active" id="open-tab"><a>open (<%= @user.bugs.length %>)</a></li>
          <li role="presentation" class="user-tab" id="pending-tab"><a>pending (<%= @user.bugs.length %>)</a></li>
          <li role="presentation" class="user-tab" id="closed-tab"><a>closed (<%= @user.bugs.length %>)</a></li>
        </ul>
        <div class="tabs-content" id="open">
          <% if @user.bugs %>
            <% @user.bugs.each do |bug| %>
              <div class="col-xs-2 col-md-1 center">
                <p><strong><%= link_to bug.bugzilla_id, bug_path(bug) %></strong></p>
              </div>
              <div class="col-xs-10 col-md-11">
                <p><%= bug.summary %></p>
              </div>
            <% end %>
          <% else %>
            <div class="col-xs-12">
              <p class="text-muted"><em>No open bugs at this time.</em></p>
            </div>
          <% end %>
        </div>
        <div class="tabs-content hidden" id="pending">
          <% if @user.bugs %>
            <% @user.bugs.each do |bug| %>
              <div class="col-xs-2 col-md-1 center">
                <p><strong><%= link_to bug.bugzilla_id, bug_path(bug) %></strong></p>
              </div>
              <div class="col-xs-10 col-md-11">
                <p><%= bug.summary %></p>
              </div>
            <% end %>
          <% else %>
            <div class="col-xs-12">
              <p class="text-muted"><em>No pending bugs at this time.</em></p>
            </div>
          <% end %>
        </div>
        <div class="tabs-content hidden" id="closed">
          <% if @user.bugs %>
            <% @user.bugs.each do |bug| %>
              <div class="col-xs-2 col-md-1 center">
                <p><strong><%= link_to bug.bugzilla_id, bug_path(bug) %></strong></p>
              </div>
              <div class="col-xs-10 col-md-11">
                <p><%= bug.summary %></p>
              </div>
            <% end %>
          <% else %>
            <div class="col-xs-12">
              <p class="text-muted"><em>No closed bugs at this time.</em></p>
            </div>
          <% end %>
        </div>
      </div>
    </div>
    <div class="row">
      <div class="col-xs-11"><hr/></div>
      <div class='col-xs-12'>
        <div class='col-xs-11'>
          <h4>Metrics</h4>
        </div>
      </div>
      <div class="col-lg-4 col-md-6">
      </div>
      <div class="col-lg-4 col-md-6">
        <p class="small">
        </p>
      </div>
      <div class="col-lg-4 col-md-6">
      </div>

    </div>
    <div class="footer-spacer"></div>

  </div>
  <%= link_to users_path, class: "col-xs-1 back-btn center" do %>
    <div class="back-triangle"><p class="sr-only">back</p></div>
  <% end %>
</div>
<div class="spacer"></div>