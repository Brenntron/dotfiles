#!/usr/bin/env ruby

require 'rubygems'
require 'daemons'

ENV['APP_ROOT'] ||= File.expand_path(File.join(File.dirname(__FILE__), '..'))
APP_ROOT = ENV['APP_ROOT']
script_file = File.join(APP_ROOT, 'lib', 'committer.rb')
tmp_dir = File.join(APP_ROOT, 'tmp')

options = {
  :app_name   => "committer",
  :dir_mode   => :normal,
  :dir        => tmp_dir,
  :multiple   => false,
  :ontop      => false,
  :mode       => :load,
  :backtrace  => true,
  :monitor    => true,
  :log_output => true
}

Daemons.run(script_file,options)
